<div class="item-form">
    <div class="header">
        {{header}}
    </div>
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.Category"}}
        </div>
        <div class="inputs">
            <select name="system.category">
                {{#select system.category}}
                <option value=""></option>
                {{#each config.spellCategories as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    {{#if system.category}}
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.Type"}}
        </div>
        <div class="inputs">
            <select name="system.type">
                {{#select system.type}} {{#each config.spellTypes as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.Range"}}
        </div>
        <div class="inputs">
            <select name="system.range">
                {{#select system.range}} {{#each config.spellRanges as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Duration"}}
        </div>
        <div class="inputs">
            <select name="system.duration">
                {{#select system.duration}} {{#each config.durations as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    <div class="form-line">
        <div class="label">
            {{localize "SR5.DrainValue"}}
        </div>
        <div class="inputs">
            <input
                type="text"
                maxlength="2"
                size="2"
                name="system.drain"
                value="{{system.drain}}"
                data-dtype="Number"
                placeholder="-2"
                data-dtype="Number"
            />
        </div>
    </div>
    {{#ife system.category "combat"}}
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.CatCombat"}}
        </div>
        <div class="inputs">
            <select name="system.combat.type">
                {{#select system.combat.type}} {{#each config.combatSpellTypes as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    {{/ife}} {{#ife system.category "detection"}}
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.CatDetection"}}
        </div>
        <div class="inputs between">
            <select name="system.detection.passive" data-dtype="Boolean">
                {{#select system.detection.passive}}
                <option value="false">{{localize "SR5.Active"}}</option>
                <option value="true">{{localize "SR5.Passive"}}</option>
                {{/select}}
            </select>
            <select name="system.detection.type">
                {{#select system.detection.type}} {{#each config.detectionSpellTypes as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
            <label>
                <input
                    class="display"
                    type="checkbox"
                    name="system.detection.extended"
                    {{checked system.detection.extended}}
                />{{localize "SR5.DetectionSpellExtended"}}
            </label>
        </div>
    </div>
    {{/ife}} {{#ife system.category "illusion"}}
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.CatIllusion"}}
        </div>
        <div class="inputs between">
            <select name="system.illusion.type">
                {{#select system.illusion.type}} {{#each config.illusionSpellTypes as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
            <select name="system.illusion.sense">
                {{#select system.illusion.sense}} {{#each config.illusionSpellSenses as |name type|}}
                <option value="{{type}}">{{localize name}}</option>
                {{/each}} {{/select}}
            </select>
        </div>
    </div>
    {{/ife}} {{#ife system.category "manipulation"}}
    <div class="form-line">
        <div class="label">
            {{localize "SR5.Spell.CatManipulation"}}
        </div>
        <div class="inputs">
            <label>
                {{localize "SR5.Spell.ManipulationDamaging"}}
                <input
                    class="display"
                    type="checkbox"
                    name="system.manipulation.damaging"
                    {{checked system.manipulation.damaging}}
                />
            </label>
            <label>
                {{localize "SR5.Spell.ManipulationMental"}}
                <input
                    class="display"
                    type="checkbox"
                    name="system.manipulation.mental"
                    {{checked system.manipulation.mental}}
                />
            </label>
            <label>
                {{localize "SR5.Spell.ManipulationEnvironmental"}}
                <input
                    class="display"
                    type="checkbox"
                    name="system.manipulation.environmental"
                    {{checked system.manipulation.environmental}}
                />
            </label>
            <label>
                {{localize "SR5.Spell.ManipulationPhysical"}}
                <input
                    class="display"
                    type="checkbox"
                    name="system.manipulation.physical"
                    {{checked system.manipulation.physical}}
                />
            </label>
        </div>
    </div>
    {{/ife}} {{/if}}
</div>
