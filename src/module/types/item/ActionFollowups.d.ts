declare namespace Shadowrun {
    /**
     * A follow up action descriptor. Meant to be a user intractable entry point providing
     * all that's needed to create a follow up test.
     */
    interface FollowupAction {
        label: string
    }
}