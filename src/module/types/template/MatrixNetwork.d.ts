/// <reference path="../Shadowrun.d.ts" />

declare namespace Shadowrun {
    export interface MatrixNetworkData {
        // TODO: Should this be the global data point for all matrix network visibilities?
        visible: boolean
    }
}
