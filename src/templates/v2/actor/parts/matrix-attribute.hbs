<div class="attribute" data-attribute-id="{{key}}" data-context-menu="attribute">
    {{#if isCyberdeck}}
        <label {{#if @root.matrixAttributeIconsOnly}}data-tooltip="{{localize label}}"{{/if}}>
            {{#if @root.matrixAttributeIconsOnly}}
                <i class="{{lookup @root.config.matrixAttributeIcons key}}"></i>
            {{/if}}
            <select class="matrix-att-selector sr5-select" name="matrix-{{key}}" data-att="{{key}}">
                {{selectOptions (matrixChoices item matrixAtt) selected=matrixAtt.device_att  }}
            </select>
        </label>
    {{else}}
        <label {{#if @root.matrixAttributeIconsOnly}}data-tooltip="{{localize label}}"{{/if}}>
            {{#if @root.matrixAttributeIconsOnly}}
                <i class="{{lookup @root.config.matrixAttributeIcons key}}"></i>
            {{else}}
                {{localize label}}
            {{/if}}
        </label>
    {{/if}}
    <div class="attribute-value">
        {{#if @root.isEditMode}}
            <label>{{matrixAtt.base}}</label>
        {{else}}
            <button class="icon sr5-icon"
                    type="button"
                    data-action="rollAttribute"
                    data-attribute-modifier-tooltip="{{key}}">{{matrixAtt.value}}</button>
        {{/if}}
    </div>
</div>
