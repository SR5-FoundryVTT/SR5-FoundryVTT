<button
    {{#if id}}id="{{id}}"{{/if}}
    class="ui-control icon template-preview-button {{actionClass}}"
    type="button"
    data-tooltip="{{tooltip}}"
    aria-label="{{localize tooltip}}"
>
    <i class="fa-solid fa-bullseye"></i>
</button>
