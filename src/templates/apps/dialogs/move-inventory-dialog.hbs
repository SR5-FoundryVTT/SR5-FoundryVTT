<div class="move-inventory-dialog-content">
    {{#each inventories}}
    <div>
        <input type="radio" id="{{this.name}}" name="inventories" value="{{this.name}}" {{#if @first}}checked{{/if}}>
        <label for="{{this.name}}">{{localize this.label}}</label>
    </div>
    {{/each}}
</div>
