{{> 'systems/shadowrun5e/dist/templates/common/List/ListHeader.hbs'
        name=(localize 'SR5.Echoes')
        itemId='echo'
        icons=(ItemHeaderIcons 'echo')
        rightSide=(ItemHeaderRightSide 'echo')
}}
{{#each itemType.echo as |item i|}}
    {{> 'systems/shadowrun5e/dist/templates/common/List/ListItem.hbs'
            img=item.img
            name=item.name
            itemId=item._id
            icons=(ItemIcons item)
            rightSide=(ItemRightSide item)
            hasDesc="true"
            hasRoll="false"
            description=item.description.value
    }}
{{/each}}
